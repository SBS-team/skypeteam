= link_to "Skypeteam", root_path, :class => 'brand'
%ul.nav
  - if user_signed_in?
    %li
      Logged in as #{current_user.name}
    %li
      = link_to 'Logout', session_path
  - else
    %li
      = link_to 'Login', new_session_path
